title: mouse-event
description: opencv-mouse-test
template:
  repo: mouse-event-template
  name: mouse-event
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
